{
  "_comment": "Drop this into ~/.claude/settings.json (or merge into your existing one). Every tool call from `claude`, `claude --bg`, and Claude Code subagents will be recorded to ~/.config/agent-audit/audit-YYYY-MM-DD.jsonl. Verify with `agent-audit verify <log> --pubkey ~/.config/agent-audit/signing.pub`.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "agent-audit hook-record"
          }
        ]
      }
    ]
  }
}
